int main() {
    int arr[5];        // 声明一个长度为5的整数数组
    arr[0] = 10;       // 给数组的第一个元素赋值
    arr[1] = 20;       // 给数组的第二个元素赋值

    printf("%d\n", arr[0]);  // 输出数组的第一个元素
    printf("%d\n", arr[1]);  // 输出数组的第二个元素

    return 0;
}